import ComponentMetadata from "@mateu/shared/apiClients/dtos/ComponentMetadata";

// Metric cards travel as the component's children
export default interface Scoreboard extends ComponentMetadata {

}
