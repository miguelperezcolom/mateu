kotlin.code.style=official
org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
# The Gradle daemon JVM is pinned to JDK 21 via gradle/gradle-daemon-jvm.properties (the Android
# Gradle Plugin doesn't support JDK 24). Compilation targets are controlled by jvmToolchain(21)/konan.
