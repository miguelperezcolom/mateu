---
title: "Slots"
weight: 14
---

# Slots

A slot is a region where child views can render.

## Example

A shell or tab container exposes a slot.

Child routes render inside it.

## Relation with routing

`parentRoute` only works if parent has a slot.
