# A page definition = a component tree, typed at the root (no `modelView` — that lives on the route).
$schema: https://raw.githubusercontent.com/miguelperezcolom/mateu/master/backend/shared/uidl/uidl-schema.json
type: VerticalLayout
content:
  - type: Text
    text: Orders
    size: xl
  - type: Text
    text: This page is a bare, typed component tree — no Java class behind it.
