{
  "description": "Fase 5: abre el form de creación.",
  "root": "go",
  "variables": {},
  "actions": {
    "go": {
      "module": "vb/action/builtin/callModuleFunctionAction",
      "parameters": { "module": "[[ $functions ]]", "functionName": "newForm" },
      "outcomes": { "success": "assign" }
    },
    "assign": {
      "module": "vb/action/builtin/assignVariablesAction",
      "parameters": {
        "$application.variables.isForm": { "source": "{{ $chain.results.go.isForm }}" },
        "$application.variables.isTable": { "source": "{{ $chain.results.go.isTable }}" },
        "$application.variables.saveMsg": { "source": "{{ '' }}" }
      }
    }
  }
}
