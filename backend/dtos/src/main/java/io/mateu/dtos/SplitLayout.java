package io.mateu.dtos;

import lombok.*;

/**
 * Metadata for a split layout. Child componentIds are in the SplitLayout component itself
 */
public record SplitLayout() implements ComponentMetadata {

}
