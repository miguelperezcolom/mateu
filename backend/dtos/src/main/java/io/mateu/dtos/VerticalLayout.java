package io.mateu.dtos;

import lombok.*;

/**
 * Metadata for a vertical layout. Child componentIds are in the VerticalLayout component itself
 */
public record VerticalLayout() implements ComponentMetadata {

}
