package io.mateu.dtos;

/**
 * Metadata for a horizontal layout. Child componentIds are in the HorizontalLayout component itself
 */
public record HorizontalLayoutDto() implements ComponentMetadataDto {}
