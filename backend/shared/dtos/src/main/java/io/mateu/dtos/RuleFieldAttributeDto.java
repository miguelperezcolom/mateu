package io.mateu.dtos;

public enum RuleFieldAttributeDto {
  required,
  disabled,
  hidden,
  pattern,
  minValue,
  maxValue,
  minLength,
  maxLength,
  css,
  style,
  theme,
  errorMessage,
  description,
  none
}
