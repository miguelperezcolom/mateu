package io.mateu.uidl.data;

/** Side of the viewport a {@link Drawer} slides in from. */
public enum DrawerPosition {
  start,
  end,
  /** Docked at the bottom, full width — the Redwood "Bottom Drawer" template. */
  bottom
}
