package io.mateu.uidl.interfaces;

/**
 * Marker interface for a top-level page of {@link Content}. Used to type views that represent a
 * full routed page rather than an embedded fragment.
 */
public interface Page extends Content {}
