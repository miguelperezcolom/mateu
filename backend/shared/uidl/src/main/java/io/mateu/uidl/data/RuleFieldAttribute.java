package io.mateu.uidl.data;

public enum RuleFieldAttribute {
  required,
  disabled,
  hidden,
  pattern,
  minValue,
  maxValue,
  minLength,
  maxLength,
  css,
  style,
  theme,
  errorMessage,
  description,
  none
}
