# yaml-language-server: $schema=https://raw.githubusercontent.com/miguelperezcolom/mateu/refs/heads/master/backend/shared/uidl/routes-schema.json
# The mount's route registry, next to the definitions it routes to. Routes are RELATIVE to the mount.
routes:
  # Two routes over ONE screen, told apart by a pinned parameter — the case a decorator cannot
  # express, since it can only say "this class is at this path".
  - route: tickets/open
    viewModel: tests.test_route_registry.Tickets
    fixedParams:
      status: open
  - route: tickets/closed
    viewModel: tests.test_route_registry.Tickets
    fixedParams:
      status: closed

  # Seeded, not pinned: the request may override these.
  - route: tickets
    viewModel: tests.test_route_registry.Tickets
    defaultParams:
      status: all
      page: 1

  # One definition, two view models. shared-list.yaml declares no modelView.
  - route: catalog/books
    definition: shared-list.yaml
    viewModel: tests.test_route_registry.Books
  - route: catalog/films
    definition: shared-list.yaml
    viewModel: tests.test_route_registry.Films

  # No view model: a definition plus client-side data — the statically deployed case.
  - route: about
    definition: about.yaml

  # A parameterised route and the static sibling it must not swallow.
  - route: orders/:id
    viewModel: tests.test_route_registry.OrderDetail
  - route: orders/new
    viewModel: tests.test_route_registry.NewOrder
